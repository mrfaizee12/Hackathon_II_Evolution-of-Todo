apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-service
  namespace: todo-chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot-service
  template:
    metadata:
      labels:
        app: chatbot-service
    spec:
      containers:
      - name: chatbot-service
        image: chatbot-service:5.0.0
        imagePullPolicy: Never
        ports:
        - containerPort: 8004
        env:
        - name: PYTHONPATH
          value: "/app:/app/backend"
        - name: SERVICE_NAME
          value: "chatbot-service"
        - name: PORT
          value: "8004"
        - name: DB_URL
          valueFrom:
            secretKeyRef:
              name: todo-app-secrets
              key: DATABASE_URL
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: todo-app-secrets
              key: SECRET_KEY
        - name: ALGORITHM
          valueFrom:
            secretKeyRef:
              name: todo-app-secrets
              key: ALGORITHM
        - name: ACCESS_TOKEN_EXPIRE_MINUTES
          valueFrom:
            secretKeyRef:
              name: todo-app-secrets
              key: ACCESS_TOKEN_EXPIRE_MINUTES
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: todo-app-secrets
              key: OPENROUTER_API_KEY
        - name: EVENT_BUS_TYPE
          value: "redpanda"
---
apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: todo-chatbot
spec:
  selector:
    app: chatbot-service
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004
  type: ClusterIP